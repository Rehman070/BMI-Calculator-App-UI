import 'package:flutter/material.dart';

const BoxShadow shadow1 = BoxShadow(
    color: Colors.black38,
    spreadRadius: 0.1,
    blurRadius: 10,
    offset: Offset(0, 5));

const BoxShadow shadow2 = BoxShadow(color: Colors.white, offset: Offset(-5, 0));

const BoxShadow shadow3 = BoxShadow(color: Colors.white, offset: Offset(5, 0));
